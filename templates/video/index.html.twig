{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('video_form') }}
{% endblock %}



{% block title %}Vidéos{% endblock %}

{% block body %}
    {{ parent() }}

    <div class="container mt-5 mb-5">
        <div class="row">
            <div class="card p-3">
                <div class="col">
                    {{ form(videoForm, {'attr': {'id': 'form_video' }}) }}
{#                    {{ form(videoForm) }}#}
                </div>
            </div>
        </div>
    </div>

    <div class="container-fluid">
        <div class="row d-flex   w-100" id="videos_list">
            {% for video in videos %}
                {% include 'video/video.html.twig' %}
            {% endfor %}
        </div>
    </div>


{% endblock %}
